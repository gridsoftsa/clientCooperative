import type { ArchivalFileModel } from '~/types/archival-file'

export const ARCHIVAL_FILE_MODEL_LABELS: Record<ArchivalFileModel, string> = {
  entity_case: 'Entidad / caso',
  org_area: 'Área productora',
}
