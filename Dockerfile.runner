# Runtime: requiere .output generado en el host (ver deploy-qa.sh).
FROM node:22-alpine

WORKDIR /app

ENV NODE_ENV=production
ENV NITRO_HOST=0.0.0.0
ENV NITRO_PORT=3000

COPY .output ./.output

EXPOSE 3000

CMD ["node", ".output/server/index.mjs"]
